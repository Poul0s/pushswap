global loop
si pile A est vide:
	retourner actions
si pile A n'a que 1 ele
	push to b
si + grande valeur est le 1er ele de pile A:
	push to b
si + grande valeur est le 2e ele de pile B:
	swap
si + grande valeur est dans la premiere moitier de pile A:
	rotate a
sinon:
	rotate reverse a
	
todo a voir comment opti le tout
 
